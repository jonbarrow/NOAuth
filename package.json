{
  "name": "noauth",
  "version": "1.0.1",
  "description": "NodeJS, Electron based oAuth system based around Nintendo accounts. Uses Nintendo as a backend",
  "main": "app.js",
  "scripts": {
    "lint": "./node_modules/.bin/eslint .",
    "lint-fix": "./node_modules/.bin/eslint . --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1"
  }
}
